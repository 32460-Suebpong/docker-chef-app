# 1. ขอฐานเป็น Python เวอร์ชัน 3.9 (แบบตัวเล็กๆ slim)
FROM python:3.9-slim

# 2. เข้าไปทำงานในโฟลเดอร์ /app ในกล่อง
WORKDIR /app

# 3. ก๊อปปี้ไฟล์ทั้งหมดจากเครื่องเรา (.) ไปใส่ในกล่อง (.)
COPY . .

# 4. เมื่อกล่องเริ่มทำงาน ให้รันคำสั่งนี้...
CMD ["python", "main.py"]